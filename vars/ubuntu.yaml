packages:
  - apt-transport-https
  - build-essential

tools:
  bat: bat-cat
  delta: delta
  fzf: fzf
  fd: find-fd
  rg: ripgrep
  tealdeer: tealdeer

aliases:
  - source: "$HOME/bin/bat"
    dest: "/usr/bin/bat-cat"
  - source: "$HOME/bin/fd"
    dest: "/usr/bin/find-fd"
